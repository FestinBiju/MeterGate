# Infrastructure

Local PostgreSQL and Redis are defined in the repository-root `docker-compose.yml`. Additional deployment infrastructure and database migrations belong here only when their milestones begin.

